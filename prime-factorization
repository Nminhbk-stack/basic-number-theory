#include <bits/stdc++.h>
using namespace std;

#define ll long long

void pt(ll n){
    for(ll i = 2; i<= sqrt(n); i++){
        if(n % i == 0){
            while(n % i == 0){
                cout << i << " ";
                n /=i;
            }
        }
    }
    if (n != 1) cout << n;

}


int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    ll n; cin >> n;
    pt(n);



    return 0;
}
